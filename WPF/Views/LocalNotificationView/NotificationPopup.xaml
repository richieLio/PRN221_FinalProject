<UserControl x:Class="WPF.Views.LocalNotificationView.NotificationPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF.Views.LocalNotificationView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Visibility="{Binding IsNotificationPopupOpen, Converter={StaticResource BooleanToVisibilityConverter}}">
    
    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Background="White">
        <StackPanel>
            <TextBlock Text="Notifications" FontWeight="Bold" FontSize="16" Margin="10" HorizontalAlignment="Center"/>
            <ListBox x:Name="NotificationList" ItemsSource="{Binding Notifications}" Margin="10">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Message}" Margin="5"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Border>
</UserControl>
